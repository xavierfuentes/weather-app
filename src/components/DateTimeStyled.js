import styled from 'styled-components';

const DateTimeStyled = styled.div`
  font-size: .6em;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: .3em;
  margin: .313em 0;
`;

export default DateTimeStyled;
